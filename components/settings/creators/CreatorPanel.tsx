import React from 'react';
import { BaseSettingsPanel } from '@/components/settings/BaseSettingsPanel';

export const CreatorPanel: React.FC = () => {
  return (
    <BaseSettingsPanel title="Creator's Dashboard">
      <></>
    </BaseSettingsPanel>
  );
};
